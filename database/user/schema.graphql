type User {
    id: ID!
    userName: String
    age: Int
    gender: String
}

input UserQuery {
    userName: String
    age: Int
    gender: String
}

type UserPage implements Pagination {
    pageSize: Int
    pageNum: Int
    total: Int
    data: [User]
}
